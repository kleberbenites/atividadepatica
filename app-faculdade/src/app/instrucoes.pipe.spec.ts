import { InstrucoesPipe } from './instrucoes.pipe';

describe('InstrucoesPipe', () => {
  it('create an instance', () => {
    const pipe = new InstrucoesPipe();
    expect(pipe).toBeTruthy();
  });
});
